# Server Configuration
PORT=3000                 # API port (if 3000 is in use, you can change this)
NODE_ENV=development

# Database Configuration
DB_PORT=5432             # PostgreSQL port (if 5432 is in use, you can change this)

# Database URL Configuration
# For local development without Docker:
DATABASE_URL="postgresql://postgres:password@localhost:5432/teammanager"

# For Docker development (recommended):
# DATABASE_URL="postgresql://postgres:password@db:5432/teammanager"

# Security (Future enhancements)
# JWT_SECRET=your_jwt_secret_here
# CORS_ORIGIN=http://localhost:3000

# Optional Features
# ENABLE_REQUEST_LOGGING=true
# ENABLE_SWAGGER=true

# Note: When running with Docker, if you change PORT or DB_PORT here,
# the exposed ports in docker-compose.yml will automatically adjust